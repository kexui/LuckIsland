# LuckIsland 项目大纲

## 一、项目描述

### 项目概述

**LuckIsland（幸运岛）** 是一款基于Unity引擎开发的回合制桌面游戏，核心玩法融合了大富翁的地产经营机制与卡牌策略元素。游戏采用高度模块化的架构设计，Logic层与View层完全分离，支持未来扩展为联机多人游戏。

### 核心玩法

- **回合制流程**：玩家轮流投骰子移动，触发地块事件，使用卡牌策略
- **地图系统**：由Cube组成的地图（Land、Tile、Env），Tile与相邻Land关联，玩家在Tile上移动，可对相邻Land进行操作
- **建筑系统**：包含Start（起点）、Shop（商店）、Property（地产，可建造升级）三种建筑类型
- **经济系统**：金币管理、土地购买、租金收取、建筑升级
- **卡牌系统**：策略卡牌、事件卡牌，影响游戏进程

### 架构特点

- **逻辑表现分离**：Logic层纯C#实现，无Unity依赖，可独立测试和运行
- **事件驱动**：通过EventBus实现系统间解耦通信
- **数据驱动**：使用ScriptableObject配置游戏数据，支持序列化存档
- **网络预留**：通过接口抽象，可无缝切换本地/联网模式

### 系统架构

```
GameManager（单例，核心管理器）
├── 统一创建和初始化所有System
├── 持有System引用（属性访问）
├── 管理System初始化顺序
└── 统一生命周期控制（暂停/恢复/清理）

System层（功能系统，非单例）
├── 普通类（无Unity依赖，纯逻辑）
├── 通过GameManager.Instance访问
├── 通过构造函数注入依赖
└── 示例：MapSystem, PlayerSystem, TurnSystem, BuildingSystem, CardSystem, UISystem

EventBus（事件系统）
├── 类型安全的事件订阅/发布
├── Logic和View之间的通信桥梁
└── 观察者模式实现

Logic层（数据 + 行为）
├── 纯C#类，无Unity依赖，可序列化
├── 数据字段 + 业务方法 + 序列化方法
├── 通过System管理（如PlayerSystem管理PlayerLogic列表）
└── 示例：PlayerLogic, TileLogic, BuildingLogic

View层（表现层）
├── MonoBehaviour组件，只负责Unity表现
├── 持有Logic引用（通过System获取）
├── 订阅EventBus事件，更新Unity表现
└── 示例：PlayerView, TileView, HudUI

Command层（命令模式）
├── 封装操作命令，便于网络同步
├── 可序列化，可执行/撤销
└── 示例：MoveCommand, BuildCommand, UseCardCommand
```

### 回合控制流程

| 回合         | 子回合     | 作用       |
| ---------- | ------- | -------- |
| startTurn  |         | 出免疫卡     |
| Wait       |         | 判定，发牌    |
| RollDice   |         | 投骰子      |
| Move       | Move    | 角色移动     |
|            | Event   | 触发事件（被动） |
| PlayerTurn | player1 | 出牌       |
|            | Player2 | 出牌       |
| EndTurn    |         |          |

### 核心系统列表

#### 核心系统（必备）
- **TurnSystem**：管理回合流程、当前玩家、阶段切换
- **EventBus**：系统间解耦通信，广播事件
- **MapSystem**：管理所有cube（Land、Tile、Env）
- **DiceSystem**：投骰子逻辑、骰子点数计算、骰子事件广播
- **MoveSystem**：控制玩家移动、路径动画、触发到达事件
- **PropertySystem**：处理土地购买、收租、地产事件
- **PlayerSystem**：管理玩家数据、状态、钱、位置
- **CommandSystem**（可选）：封装玩家行为，方便回放/联机同步

#### 辅助系统（可选/扩展）
- **UIEventSystem**：监听游戏事件 → 更新 UI（金币、回合、提示）
- **AnimationSystem**：播放动画（角色移动、买地动画、骰子动画）
- **AudioSystem**：播放音效、背景音乐
- **SaveLoadSystem**：保存和加载游戏状态（玩家数据、地图状态等）

## 二、技术栈总结

### 核心架构技术

#### 1. C# 编程特性

- **泛型编程**：Singleton<T>、EventBus泛型事件系统
- **异步编程**：async/await实现回合状态流转
- **接口抽象**：IPlayerInput、IGameStateSync、ITurnManager等网络抽象层
- **序列化**：JSON序列化支持存档和网络传输
- **委托与事件**：事件系统底层支持

#### 2. 设计模式

- **单例模式**：GameManager（唯一单例），Singleton<T>基类
- **观察者模式**：EventBus类型安全的事件订阅/发布
- **状态机模式**：TurnSystem回合状态机
- **命令模式**：Command系统，支持序列化和网络同步
- **策略模式**：卡牌效果系统、建筑效果系统
- **依赖注入**：System通过构造函数注入依赖
- **工厂模式**：BuildingEffectFactory建筑效果工厂

### Unity 核心技术

#### 1. 基础Unity功能

- **MonoBehaviour**：View层组件生命周期管理
- **ScriptableObject**：配置数据（卡牌、事件、建筑、地图模板）
- **Resources系统**：资源加载管理
- **场景管理**：SceneManager场景切换
- **协程（Coroutine）**：分帧初始化、动画控制

#### 2. UI系统

- **UGUI**：Unity GUI系统，界面构建
- **DOTween**：UI动画和补间动画库
- **Canvas系统**：UI层级管理

#### 3. 网格与坐标系统

- **Grid系统**：自定义网格坐标系统（GridPos、GridDirs）
- **Cell系统**：网格单元格抽象（Cell基类）
- **GridHelper**：网格工具类，坐标转换

### 数据管理技术

#### 1. 配置数据

- **ScriptableObject**：运行时配置数据
  - MapTemplateConfig（地图模板配置）
  - MapRuntimeConfig（运行时地图配置）
  - BuildingConfig（建筑配置）
  - LandConfig（土地配置）
  - TileConfig（地块配置）

#### 2. 运行时数据

- **Data层**：MapData、TileData、LandData、BuildingData
- **Logic层**：MapLogic、TileLogic、LandLogic、BuildingLogic、PlayerLogic
- **可序列化设计**：Logic层数据支持JSON序列化

#### 3. 持久化

- **JSON序列化**：存档系统、网络传输
- **ScriptableObject**：简单配置存储（可选）

### 系统架构层

#### 1. 核心系统（Core）

- **GameManager**：单例管理器，统一管理所有System生命周期
- **SystemBase**：System基类，定义初始化、更新、清理接口
- **EventBus**：类型安全的事件系统，支持泛型事件
- **Singleton<T>**：单例模式基类
- **ILogic**：Logic层接口
- **LogicBase**：Logic基类

#### 2. 游戏系统（Game Systems）

- **TurnSystem**：回合流程管理、状态机
- **MapSystem**：地图管理、Tile和Land管理
- **PlayerSystem**：玩家数据管理、状态管理
- **BuildingSystem**：建筑管理、效果触发（规划中）
- **DiceSystem**：骰子逻辑、随机数生成（规划中）
- **MoveSystem**：玩家移动、路径计算（规划中）
- **PropertySystem**：土地购买、收租逻辑（规划中）
- **CardSystem**：卡牌管理、效果执行（规划中）
- **UISystem**：UI事件响应、界面更新

#### 3. 编辑器工具

- **MapEditorWindow**：地图编辑器窗口
- **MapGeneratorWindow**：地图生成器窗口
- **MapSaveWindow**：地图保存窗口

### 性能优化技术

#### 1. 已规划

- **对象池模式**：骰子、特效、UI元素复用
- **数据结构优化**：Dictionary快速查找（O(1)索引）
- **分帧初始化**：协程分帧加载，避免卡顿

#### 2. 可扩展

- **资源异步加载**：Addressables或AssetBundle（如需）
- **批处理优化**：DrawCall优化（如需）

### 网络架构（预留）

#### 1. 接口抽象层

- **IPlayerInput**：玩家输入抽象
- **IGameStateSync**：游戏状态同步接口
- **ITurnManager**：回合管理接口
- **本地/联网切换**：通过接口实现切换

#### 2. 同步策略

- **命令同步**：Command模式封装操作
- **状态同步**：Logic层数据序列化同步
- **事件同步**：EventBus事件网络广播（规划中）

#### 3. 网络框架

- **Mirror**：Unity网络框架（预留）

### 开发工具与技术

#### 1. 版本控制

- **Git**：版本控制系统
- **提交规范**：feat/fix/refactor等规范提交

#### 2. 代码规范

- **命名空间**：模块化命名空间组织
- **C#编码规范**：遵循C#编码标准
- **注释规范**：代码注释标准

#### 3. 测试（可扩展）

- **单元测试**：Logic层纯C#类可单元测试（NUnit/xUnit）
- **集成测试**：System集成测试（待规划）

### 扩展技术栈（未来可能）

#### 1. AI系统

- **状态机AI**：AI决策状态机
- **行为树**：复杂AI行为

#### 2. 音频系统

- **AudioSystem**：音效、背景音乐管理（规划中）
- **Unity Audio**：AudioSource、AudioClip管理

#### 3. 特效系统（最后弄）

- **粒子系统**：Unity Particle System
- **动画系统**：Animator、Animation Clip

#### 4. 存档系统

- **SaveLoadSystem**：完整的存档加载系统（规划中）

## 三、技术栈汇总表

| 类别 | 技术/工具 | 状态 | 说明 |
|------|----------|------|------|
| **编程语言** | C# | ✅ 已使用 | Unity主要开发语言 |
| **引擎** | Unity | ✅ 已使用 | 游戏引擎 |
| **架构模式** | Logic-View分离 | ✅ 已使用 | 核心架构设计 |
| **设计模式** | 单例/观察者/状态机/命令/策略 | ✅ 已使用 | 多种设计模式应用 |
| **事件系统** | EventBus（自定义） | ✅ 已使用 | 类型安全事件系统 |
| **UI框架** | UGUI | ✅ 已使用 | Unity UI系统 |
| **动画库** | DOTween | ✅ 已使用 | UI动画 |
| **配置系统** | ScriptableObject | ✅ 已使用 | 数据驱动配置 |
| **序列化** | JSON | 📋 规划中 | 存档和网络传输 |
| **网格系统** | 自定义Grid | ✅ 已使用 | 地图网格坐标 |
| **编辑器工具** | Custom Editor Windows | ✅ 已使用 | 地图编辑器 |
| **版本控制** | Git | ✅ 已使用 | 代码管理 |
| **对象池** | ObjectPool（规划） | 📋 规划中 | 性能优化 |
| **网络框架** | Mirror | 📋 预留 | Unity网络框架 |
| **资源管理** | Resources/Addressables | 📋 部分使用 | 资源加载 |
| **AI系统** | 状态机/行为树 | 📋 可扩展 | AI决策 |
| **音频系统** | Unity Audio | 📋 规划中 | 音效和背景音乐 |

---

**说明**：

- ✅ 已使用：已经在项目中实现和使用
- 📋 规划中：已规划但尚未实现
- 📋 预留：为未来扩展预留的接口/架构
- 📋 可扩展：可选的技术方案，根据需要添加

## 四、设计原则

- **联机独立**：Logic层无Unity依赖，可独立运行和测试
- **表现独立**：View层只负责显示，不包含游戏逻辑
- **数据独立**：Logic层数据可序列化，便于网络传输
- **后期扩展联机**：通过接口抽象，可无缝切换本地/联网模式

